Help:
Remove dependencies on the source of git repositories which use the Assertions C++ Framework

Usage: ./dependencies.sh remove git GIT_URL

GIT_URL: HTTP URL used to clone the git repository. This is the same URL used for adding the dependency
